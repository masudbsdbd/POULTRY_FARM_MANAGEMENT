<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Balance Sheet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 10px;
            background-color: #f4f4f4;
            font-size: 12px;
        }
        .container {
            max-width: 1000px;
            background: white;
            padding: 15px;
            margin: auto;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }
        h2 {
            text-align: center;
            margin-bottom: 5px;
        }
        .header {
            text-align: center;
            font-weight: bold;
            font-size: 12px;
            margin-bottom: 10px;
        }
        .section {
            margin-top: 10px;
        }
        .section-title {
            background: #888;
            color: white;
            padding: 3px 6px;
            font-weight: bold;
            font-size: 12px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 5px;
            font-size: 11px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 5px 6px;
            text-align: left;
        }
        th {
            background: #eee;
        }
        .total {
            font-weight: bold;
            background-color: #f9f9f9;
        }
        .highlight {
            background-color: #f1f1f1;
            font-weight: bold;
        }
        .amount-small-right {
            font-size: 10px;
            text-align: right;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Balance Sheet</h2>
        <p class="header">As of {{ request('date') }}</p>
        
        <div class="section">
            <div class="section-title">Assets</div>
            <table>
                <tr><th>Current Assets</th><th>Amount</th></tr>
                <tr><td>Cash</td><td class="amount-small-right">{{ $currentCashAmount }}</td></tr>
                <tr><td>Bank</td><td class="amount-small-right">{{ $currentBankAmount }}</td></tr>
                <tr><td colspan="2"><strong>Accounts Receivable</strong></td></tr>
                @foreach($totalReceivableAmountSeparator as $item)
                <tr>
                    <td>{{ $item->receivable_head_name }}</td>
                    <td class="amount-small-right">{{ showAmount($item->total_amount) }} Tk</td>
                </tr>
                @endforeach
                <tr class="highlight">
                    <td>Total Accounts Receivable</td>
                    <td>{{ showAmount($totalReceivableAmount) }} Tk</td>
                </tr>
                <tr><td>Inventory</td><td class="amount-small-right">{{ $totalPurchaseStock - $totalDamageAmount }}</td></tr>
                <tr class="highlight">
                    <td>Total Current Assets</td>
                    <td>{{ $currentCashAmount + $currentBankAmount + $totalReceivableAmount + $totalPurchaseStock }}</td>
                </tr>
            </table>

            <table>
                <tr><th>Fixed Assets</th><th>Amount</th></tr>
                <tr><td>Total Fixed Assets</td><td class="amount-small-right">{{ $totalAssets }}</td></tr>
                <tr class="highlight">
                    <td>Total Assets</td>
                    <td>{{ $totalAssets + $currentCashAmount + $currentBankAmount + $totalReceivableAmount + $totalPurchaseStock }}</td>
                </tr>
            </table>
        </div>

        <div class="section">
            <div class="section-title">Liabilities</div>
            <table>
                <tr><th>Liabilities</th><th>Amount</th></tr>
                <tr>
                @foreach($totalPayableAmountSeparator as $item)
                <tr>
                    <td>{{ $item->payables_head_name }}</td>
                    <td class="amount-small-right">{{ showAmount($item->total_amount) }} Tk</td>
                </tr>
                @endforeach
                </tr>
                <tr class="highlight">
                    <td>Total Liabilities</td>
                    <td>{{ $totalPayableAmount  }}</td>
                </tr>
            </table>
        </div>

        <div class="section">
            <div class="section-title">Equity</div>
            <table>
                <tr><th>Equity</th><th>Amount</th></tr>
                <tr>
                    <td>Owner's Capital</td>
                    <td class="amount-small-right">
                        {{ 
                            ( 
                                ($totalAssets + $currentCashAmount + $currentBankAmount + $totalReceivableAmount + $totalPurchaseStock) 
                                - ($incomes + $totalSellProfit) 
                                - ($totalPayableAmount) 
                            ) 
                        }}
                    </td>
                </tr>
                <tr><td>Other's Earnings</td><td class="amount-small-right">{{ $incomes + $totalSellProfit }}</td></tr>
                <tr class="highlight">
                    <td>Liabilities & Equity Total</td>
                    <td>
                        {{ 
                            (
                                (
                                    (
                                        ($totalAssets + $currentCashAmount + $currentBankAmount + $totalReceivableAmount + $totalPurchaseStock)
                                        - ($incomes + $totalSellProfit)
                                        - ($totalPayableAmount)
                                    ) 
                                    + ($incomes + $totalSellProfit)
                                ) 
                                + ($totalPayableAmount)
                            ) 
                        }}
                    </td>
                </tr>
            </table>
        </div>
    </div>
</body>
</html>
